<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_class_for(type) %> fade in alert-dismissable">
    <button class="close" data-dismiss="alert"><span>&times;</span></button>
    <%= message %>
  </div>
<% end %>